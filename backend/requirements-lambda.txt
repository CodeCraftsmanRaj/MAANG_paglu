# Runtime dependencies for the AWS Lambda package (python3.12, x86_64).
# Deliberately excludes:
#   fastembed  — heavy ONNX runtime; production uses Bedrock Titan embeddings.
#                (FastEmbedProvider lazily falls back to hashed vectors if imported.)
#   uvicorn    — HTTP server; Mangum bridges API Gateway/Lambda events to the ASGI app.
fastapi>=0.110.0
pydantic>=2.6.0
httpx>=0.27.0
requests>=2.31.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
numpy>=1.26.0
networkx>=3.2
boto3>=1.34.0
mangum>=0.22.0
